import React from 'react';
import auth from '../../util/firebase.config';

const Header = () => {
    return (
        <div>
            <h1>Hi Header</h1>
        </div>
    );
};

export default Header;